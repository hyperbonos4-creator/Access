# urban-vision — dependencias (pinned).
# Nota: para GPU NVIDIA en producción, sustituir `onnxruntime` por
# `onnxruntime-gpu` (mismo pin) y construir con VISION_EXECUTION_PROVIDER=cuda.
# Ver README §GPU.

# ── API ───────────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# ── Vector store ───────────────────────────────────────────────────────────
qdrant-client==1.12.2

# ── Inferencia (detección + embeddings) ────────────────────────────────────
# insightface trae SCRFD (det) + ArcFace (rec) sobre onnxruntime.
numpy==1.26.4
onnxruntime==1.20.1
insightface==0.7.3
opencv-python-headless==4.10.0.84

# ── OCR de documentos (KYC, spec resident-onboarding-kyc) ───────────────────
# RapidOCR = modelos PP-OCRv4 (PaddleOCR) ejecutados sobre el MISMO onnxruntime
# ya presente. Sin dependencia de PaddlePaddle (build liviano), 100% local (Ley
# 1581) y con pesos incluidos en el wheel. Carga perezosa en app/pipeline/ocr.py.
rapidocr-onnxruntime==1.4.4

# ── Tests (G0 1.4) ──────────────────────────────────────────────────────────
pytest==8.3.4
httpx==0.28.1

# ── LPR (G6, opcional) — descomentar para habilitar lectura de placas ─────────
# Se importa de forma perezosa (app/pipeline/lpr.py); no bloquea el pipeline
# facial si no está instalado.
# fast-alpr==0.1.0
